"""Travel Agent core package.

This package holds the reusable application logic for the travel discovery
tool. It is intentionally kept separate from any user-facing interface
(CLI today, web framework later) so that a new interface can be added on
top without restructuring the underlying code.
"""
