"""FastAPI web layer for the Travel Discovery Tool.

This package translates HTTP requests into calls against
`travel_agent.core` and translates results/exceptions into JSON
responses. All business logic stays in `travel_agent.core`.
"""
